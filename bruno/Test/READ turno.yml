info:
  name: READ turno
  type: http

http:
  method: GET
  url: http://localhost:3000/api/v1/turnos-reserva/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​