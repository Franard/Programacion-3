info:
  name: ADD medicos
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/medicos
  body:
    type: json
    data: |-
      {
        "id_usuario": 1, 
        "id_especialidad": 1,
        "matricula": "5000",
        "descripcion": "Especialista en cardiología clínica",
        "valor_consulta": 15000.50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
